name: Email Action Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 24f2005139@ds.study.iitm.ac.in
        run: echo "Hello, world! This step contains the email address."

      - name: Display completion message
        run: echo "GitHub Action workflow completed successfully!"
